server:
  port: 8762
spring:
  profiles:
    active: native
  application:
    name: nativeconfigserver
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
